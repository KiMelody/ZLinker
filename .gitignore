# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Screenshot tooling
tool/node_modules/
# UI mock-first workflow scratch (HTML mockups, never shipped — see AGENTS.md)
tool/design/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
.cdp/

# Local desktop-preview artifacts (windows runner is generated by
# `flutter run -d windows`; not a release platform — see AGENTS.md)
/android/build/
/windows/

# Release signing credentials (never commit; CI uses secrets instead)
/android/key.properties
/android/zlinker-release.jks

#ai
.zcode
.trellis
.gitattributes

# Local-only agent notes (kept out of history at the user request, 2026-09-16)
/AGENTS.md

# Local-only architecture/domain docs (2026-09-17, per user request)
/CONTEXT.md
/docs/adr/
